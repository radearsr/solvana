[
  {
    "name": "ENABLE_USER_NON_ACTIVE",
    "description": "Permintaan untuk mengaktifkan kembali pengguna yang nonaktif, dan pengguna yang pernah hapus akun",
    "apiEndpoint": "http://localhost:3000/api/v1/unitedpay/enable-user-nonactive",
    "requiredBody": {
      "kode_agen": "string"
    }
  },
  {
    "name": "OPEN_KYC_IMAGE_UNITEDPAY",
    "description": "Permintaan terkait verifikasi dokumen atau data (KYC) dan aset gambar yang berada dibackoffice (BO) untuk UNITEDPAY dengan kode agen awalan UTD",
    "apiEndpoint": "http://localhost:3000/api/v1/unitedpay/upload-verification-image",
    "requiredBody": {
      "kode_agen": "string"
    }
  },
  {
    "name": "CREATE_VOUCHER_MAARI",
    "description": "Permintaan untuk membuat voucher di MAARI dengan cabang (Madukoro/Ventura)",
    "apiEndpoint": "http://localhost:3000/api/v1/esb/voucher/excel",
    "requiredBody": {
      "vouchersConfig": [
        {
          "cabang": "string",
          "detailsVouchers": [
            {
              "voucherCode": "string",
              "voucherLengthDays": "string (hari berlaku voucher, isi 1 jika tidak ada perintah)",
              "minSalesAmount": "string(isi 0 jika tidak ada perintah)",
              "amount": "string",
              "notes": "string (kesimpulan dari perintah untuk apa voucher ini atau isi dengan nominal dan cabang mana)",
              "startDate": "string (DD/MM/YYYY)",
              "endDate": "string (DD/MM/YYYY)"
            }
          ]
        }
      ]
    }
  },
  {
    "name": "OPEN_KYC_IMAGE_MURAPAY",
    "description": "Permintaan terkait verifikasi dokumen atau data (KYC) dan aset gambar yang berada dibackoffice (BO) untuk MURAPAY dengan kode agen awalan MP",
    "apiEndpoint": "http://localhost:3000/api/v1/murapay/upload-verification-image",
    "requiredBody": {
      "kode_agen": "string"
    }
  },
  {
    "name": "CREATE_SCHEDULE",
    "description": "Permintaan untuk membuat jadwal",
    "apiEndpoint": "https://api.solvana.id/create-schedule",
    "requiredBody": {
      "schedule": "string",
      "task": "string"
    }
  },
  {
    "name": "COMMON_CHAT",
    "description": "Percakapan biasa yang tidak memiliki arti dalam kasus yang diberikan diatas",
    "apiEndpoint": "http://192.168.0.9:5000/ask",
    "requiredBody": {
      "query": "string"
    }
  }
]
